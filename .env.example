# PostgreSQL connection string (local docker-compose or Neon)
DATABASE_URL="postgresql://hotel:hotel@localhost:5433/hotel_management?schema=public"

# Secret used to sign JWT session tokens (generate: openssl rand -hex 32)
AUTH_SECRET="replace-with-a-long-random-string"
